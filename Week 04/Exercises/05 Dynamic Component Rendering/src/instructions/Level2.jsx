const Level2 = () => {
  return (
    <>
      <h4>Passing and displaying data</h4>
      <ul>
        <li>
          The components are now displayed, however no data from the user is
          showing yet. Pass the <b>current user</b> of every array iteration as{' '}
          <b>props</b> to the User component.
        </li>
        <li>
          In <b>User.js</b>, use the props given to display some of the users
          data. Pay close attention to the <b>structure</b> the data has.
        </li>
        <li>Display the props that are relevant in the User component.</li>
      </ul>
    </>
  );
};

export default Level2;
